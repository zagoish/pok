# 宝可梦宝石联赛

一个私用的宝可梦主题《璀璨宝石》基础版单机网页原型。

## 运行

```powershell
cd web
npm install
npm run dev
```

测试和构建：

```powershell
cd web
npm test -- --run
npm run build
```

项目不依赖后端或外部 AI 服务，电脑对手使用 TypeScript 规则策略。
